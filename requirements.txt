# --- сбор ---
twscrape>=0.13          # скрейпинг X через залогиненные аккаунты

# --- конфиги ---
pyyaml>=6.0

# --- лемматизация иврита ---
# Бэкенд по умолчанию. Тянет torch (~2.5 ГБ) + модель he (~400 МБ).
# Без него конвейер работает, но откатывается на грубый режим rules
# и честно сообщает об этом в отчёте.
stanza>=1.9

# --- опционально: бэкенд лемматизации dicta (API) ---
# httpx>=0.27
